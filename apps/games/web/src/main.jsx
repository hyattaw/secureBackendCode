import React from "react";
import ReactDOM from "react-dom/client";
import App from "@drewhyatt/ui/App";
import "@drewhyatt/ui/styles/style.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
